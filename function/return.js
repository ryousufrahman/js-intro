// we can store the function output/result to a varriable and we can do what we want to do with that varriable later

function tenTimes(num1) {
    const result= num1*10;
    return result;
    
}

const tenTimesResult = tenTimes(55);
console.log(tenTimesResult);

const tenTimesResult2 =tenTimes(88);
console.log(tenTimesResult2);